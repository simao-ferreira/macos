# Environment
alias reloadzsh='source $ZDOTDIR/.zshrc'

# Directories
alias la='ls -lAh'
alias ll='ls -lh'

# Vim
alias v='nvim'
alias vim='nvim'

# Copy and move
alias cp='cp -iv'
alias mv='mv -iv'

# Print each PATH entry on a separate line
alias path="echo -e \"${PATH//:/\\n}\""
